terraform fmt
terraform validate
terraform init
terraform plan
terraform apply
terraform destroy
terraform plane --auto-approve(without asking permission)
terraform apply --auto-approve
terraform validate(its check the syntax and error)
terraform fmt(its setup the hcl indendation and code smell)
SIMPLY REMEMBER IPAD I=init P= plan A=apply D=destroy
#workspaces 
terraform workspace
Subcommands:
    delete    Delete a workspace
    list      List Workspaces
    new       Create a new workspace
    select    Select a workspace
    show      Show the name of the current workspace
terraform state list(its list down the resources)
#replace
terraform apply -replace=<resource-name>
terraform apply -replace='aws_instance.web["app"]'
#replace=>its recreate the resource
#taint
terraform taint <resource-name>
terraform untaint <resourcename>
#both replace and taint are done by the same functionality but taint old command and replace is the new command
 terraform apply -help(to get more commands with usage)
# target
terraform destroy -target=<resource>(its can be used to specific resource to be delete)
#First, get the EC2 instance ID (for example, i-0123456789abcdef0) from the AWS Console or CLI.
when u delete unfortunatily congfiguration file
Create a minimal configuration
provider "aws" {
  region = "us-east-1"
}

resource "aws_instance" "old" {
  ami           = "ami-06067086cf86c58e6" # Placeholder
  instance_type = "t3.micro"              # Placeholder
}
terraform init
Then run:
terraform import aws_instance.old i-0123456789abcdef0
